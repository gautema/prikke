<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title default="Prikke" suffix=" · Prikke">
      {assigns[:page_title]}
    </.live_title>
    <link rel="icon" type="image/svg+xml" href={~p"/favicon.svg"} />
    <link phx-track-static rel="stylesheet" href={~p"/assets/css/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/js/app.js"}>
    </script>
    <script>
      (() => {
        const setTheme = (theme) => {
          if (theme === "system") {
            localStorage.removeItem("phx:theme");
            document.documentElement.removeAttribute("data-theme");
          } else {
            localStorage.setItem("phx:theme", theme);
            document.documentElement.setAttribute("data-theme", theme);
          }
        };
        if (!document.documentElement.hasAttribute("data-theme")) {
          setTheme(localStorage.getItem("phx:theme") || "system");
        }
        window.addEventListener("storage", (e) => e.key === "phx:theme" && setTheme(e.newValue || "system"));
        
        window.addEventListener("phx:set-theme", (e) => setTheme(e.target.dataset.phxTheme));
      })();
    </script>
  </head>
  <body class="bg-slate-50 min-h-screen">
    <%= unless assigns[:hide_header] do %>
      <header class="bg-white border-b border-slate-200">
        <nav class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 h-14 flex items-center justify-between">
          <a href="/" class="flex items-center gap-2 text-lg font-semibold text-slate-900 no-underline">
            <div class="w-3 h-3 bg-emerald-500 rounded-full"></div>
            prikke
          </a>
          <div class="flex items-center gap-6">
            <%= if @current_scope do %>
              <span class="text-sm text-slate-600">{@current_scope.user.email}</span>
              <.link href={~p"/users/settings"} class="text-sm text-slate-600 hover:text-slate-900">Settings</.link>
              <.link href={~p"/users/log-out"} method="delete" class="text-sm text-slate-600 hover:text-slate-900">Log out</.link>
            <% else %>
              <.link href={~p"/users/register"} class="text-sm text-slate-600 hover:text-slate-900">Register</.link>
              <.link href={~p"/users/log-in"} class="text-sm font-medium text-white bg-emerald-500 hover:bg-emerald-600 px-4 py-2 rounded-md transition-colors">Log in</.link>
            <% end %>
          </div>
        </nav>
      </header>
    <% end %>
    {@inner_content}
  </body>
</html>
